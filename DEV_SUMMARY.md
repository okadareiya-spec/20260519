# ライオン先輩Bot　開発サマリ

作成日：2026-05-27  
対象：開発レビュー・今後の開発指針

---

## ① 要件定義・設計段階で予見できた変更と、次回以降のUX設計への活かし方

### 今回発生した「後からの変更」

初回実装後に以下の2点を変更した。

| 変更内容 | 変更理由 |
|---|---|
| フェーズⅢのCore of Core参照促しのロジック改善 | 「あわせて読んでください」という付け足しになっており、アクションと原則の因果関係が断絶していた |
| ヒアリング6回到達時の処理（延長確認）と、解決策提案後のカウントリセット | 6回到達で会話が唐突に終了していた。ユーザーが継続意思を示す手段がなかった |

### なぜ初回で予見できなかったか

どちらも**実装後に動かしてみて初めて気づくタイプの問題**ではなく、設計段階で「ユーザーがその場面に立ったとき何を感じるか」を問えば気づけた問題だった。

| 問題の本質 | 設計段階で問うべきだった問い |
|---|---|
| 参照促しの動機づけ不足 | 「ユーザーはなぜ原則を読もうと思うのか？」「アクションと原則の間に因果はあるか？」 |
| 会話の強制終了 | 「6回でヒアリングが足りないと感じたユーザーはどうするか？」「解決策が出た後、次の相談はどこから始まるか？」 |

これらは**コンテンツ設計（何を伝えるか）**と**フロー設計（会話の境界条件）**の問題であり、プログラム実装より前に検討すべき領域だった。

---

### 次回の要件定義・設計でUX設計を組み込む方法

#### Claude Codeのスキル活用方針

| フェーズ | 使うスキル | 目的 |
|---|---|---|
| 要件定義 | `/doc-coauthoring` | ユーザーとの対話を通じて要件を構造化。「読者テスト」フェーズで仕様の穴を事前に検出 |
| UXフロー設計 | `/feature-dev` | コードを書く前に「ユーザーが何を見て、何を選び、何を感じるか」をフロー図として明示 |
| プロンプト設計 | `/feature-dev`（Phase 3: 質問フェーズ） | プロンプトの回答例をサンプル会話として書き起こし、境界条件（N回目・終了時・エラー時）を全列挙する |

#### 要件定義・設計時に必ず行うチェック

**① 会話フローの境界条件を全列挙する**

```
チェック項目（例）:
- 上限（ヒアリング6回）に達したとき、ユーザーは何ができるか？
- 解決策が提示された後、ユーザーが続けて相談したい場合は？
- 初回起動時 / 日付をまたいだ時 / ロールが未設定の時の挙動は？
- エラーが起きたとき（API障害等）ユーザーに何が見えるか？
```

**② コンテンツの「なぜ」を設計する**

単に「〇〇を表示する」ではなく、**ユーザーがその表示を見て何を思い、次に何をするか**まで書く。

```
悪い例：「解決策の末尾にCore of Coreの参照を促す文を入れる」
良い例：「提案したアクションのロジックの源泉が原則にあることを示し、
         ユーザーが自分の状況を重ねながら読み返したくなる文脈を作る」
```

**③ サンプル会話を3〜5本書いてから実装する**

プロンプトを実装する前に、想定される会話例を自然言語で書き起こす。  
「このやりとりは自然か？」「ユーザーは次に何をするか？」を確認してから実装に入る。

**④ `/doc-coauthoring` スキルのReader Testingを活用する**

仕様書をClaudeに渡して「ユーザーとしてこのBotを使うときに不明瞭な点は何か」を問う。  
設計段階でUX上の穴を事前に潰せる。

---

## ② 今回の開発をもとに今後に活かすポイントと改善点

### 良かった点（今後も継続する）

**アーキテクチャ設計**

| 判断 | 理由 |
|---|---|
| `prompts/advisor.py` / `prompts/teacher.py` に分離 | ロールごとにプロンプトを独立させることで、片方の変更が他方に影響しない |
| `system_prompt.py` をファサードとして使う | URL・ページ番号など変わりやすい定数を1か所に集約。変更コストが最小 |
| 起動時にプロンプトをビルドしておく | リクエストごとに生成するより効率的。プロンプトは静的なので問題なし |
| Redis キーに日付を含める | 当日セッション管理がシンプルに実現できる（TTL設定不要でも機能する）|

**開発プロセス**

| 判断 | 理由 |
|---|---|
| `CLAUDE.md` にプロジェクトルールを記載 | 「コード修正→MDも更新」のルールを明文化し、将来の開発者（自分含む）が忘れない |
| `feature/system-prompt-refine` ブランチを切って開発 | mainを壊さずに反復できた |
| MD・PNG・コードを同一コミットにまとめる | 変更内容とドキュメントの整合性が保証される |

---

### 改善点（次回以降に直す）

**① UXフロー設計を実装前のフェーズとして明示する**

今回は実装→動作確認→UX問題発覚→修正というサイクルになった。  
次回は以下の順序を守る：

```
要件定義
  → UXフロー設計（会話フロー図 ＋ 境界条件一覧 ＋ サンプル会話）
  → プロンプト設計レビュー
  → 実装
  → 動作確認
```

**② プロンプト変更にはサンプル出力の確認ステップを入れる**

プロンプトを変更したあと、「実際にClaude APIを呼んで出力を確認する」ステップを踏む。  
特にフェーズⅢのような複合ロジックは、変更前後で出力を比較することで意図通りかを検証できる。

**③ 機能の追加と改善を分けて管理する**

今回は「機能追加」（先生役・リッチメニュー等）と「品質改善」（プロンプトのロジック強化）が同一ブランチで混在した。  
次回からは以下のように分ける：

| ブランチ種別 | 内容 |
|---|---|
| `feature/xxx` | 新機能の追加（動作しなかったものを動くようにする） |
| `improve/xxx` | 既存機能の品質向上（動いているがより良くする） |

これにより、レビューと差し戻しのスコープが明確になる。

**④ 仕様サマリーに「設計の判断理由」を残す**

READMEの仕様サマリーは「何をしているか」は書けているが「なぜそう設計したか」がない。  
将来の変更時に背景を失わないよう、主要な設計判断には理由を1行添えると良い。

```markdown
| 項目 | 内容 | 理由 |
|---|---|---|
| Reply Token の使い方 | 新セッション時はウェルカム＋AI返答を1回で送る | LINE Reply Token は1Webhook Event で1回限り有効 |
| ロールのTTLを7日に設定 | 会話履歴（24h）より長い | 日付またいでもロール再設定の手間をかけさせないため |
```

---

## まとめ

```
今回発生した変更の本質は、いずれも「コードの問題」ではなく「設計の問題」だった。
要件定義・設計フェーズでUX視点（ユーザーが何を感じ、何をするか）を組み込むことで、
実装後の手戻りコストを大幅に削減できる。

Claude Codeを使う場合、/doc-coauthoringと/feature-devは実装前の設計フェーズに使うスキルであり、
「コードを書く前に仕様の穴を潰す」ために積極的に活用する。
```
